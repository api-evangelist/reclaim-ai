openapi: 3.2.0
info:
  title: Reclaim Smart Series Migration API
  description: Reclaim's awesome API
  contact:
    name: Reclaim.ai Inc.
    url: http://reclaim.ai
    email: info@reclaim.ai
  license:
    name: Reclaim 9.9
    url: http://reclaim.ai
  version: '0.1'
tags:
- name: SmartSeriesMigration
paths:
  /api/smart-series-migration/reclaim-recurrence/migrate-all:
    post:
      tags:
      - SmartSeriesMigration
      summary: This is only intended for dev use to simulate the UserTaskSchedulerService…
      description: This is only intended for dev use to simulate the UserTaskSchedulerService causing the nightly scheduled migration check to run
      operationId: migrateToGen3SmartSeries
      responses:
        '200':
          description: migrateToGen3SmartSeries 200 response
          content:
            application/json:
              schema:
                type: object
      security:
      - Authorization: []
  /api/smart-series-migration/run:
    post:
      tags:
      - SmartSeriesMigration
      operationId: migrateAllToSmartSeries
      parameters:
      - name: migrateExistingAssignments
        in: query
        required: true
        schema:
          type: boolean
      responses:
        '200':
          description: migrateAllToSmartSeries 200 response
          content:
            application/json:
              schema:
                type: object
      security:
      - Authorization: []
      summary: Migrate all to smart series
      x-summary-source: derived
components:
  securitySchemes:
    Authorization:
      type: oauth2
