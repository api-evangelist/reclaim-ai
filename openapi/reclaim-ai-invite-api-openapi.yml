openapi: 3.2.0
info:
  title: Reclaim Invite API
  description: Reclaim's awesome API
  contact:
    name: Reclaim.ai Inc.
    url: http://reclaim.ai
    email: info@reclaim.ai
  license:
    name: Reclaim 9.9
    url: http://reclaim.ai
  version: '0.1'
tags:
- name: Invite
paths:
  /api/invite/{trackingCode}:
    get:
      tags:
      - Invite
      operationId: resolve
      parameters:
      - name: trackingCode
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: resolve 200 response
          content:
            application/json:
              schema:
                type: object
      summary: Resolve
      x-summary-source: derived
components:
  securitySchemes:
    Authorization:
      type: oauth2
