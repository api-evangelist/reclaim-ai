openapi: 3.2.0
info:
  title: Reclaim Google Groups API
  description: Reclaim's awesome API
  contact:
    name: Reclaim.ai Inc.
    url: http://reclaim.ai
    email: info@reclaim.ai
  license:
    name: Reclaim 9.9
    url: http://reclaim.ai
  version: '0.1'
tags:
- name: google-groups
paths:
  /api/google/groups/expand:
    get:
      tags:
      - google-groups
      operationId: expand
      parameters:
      - name: email
        in: query
        required: true
        schema:
          type: string
      - name: credentialId
        in: query
        required: true
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: expand 200 response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupExpansionView'
      security:
      - Authorization: []
      summary: Expand
      x-summary-source: derived
components:
  schemas:
    GroupExpansionView.GroupMemberView:
      required:
      - email
      - isTeamMember
      type: object
      properties:
        email:
          type: string
        userId:
          type:
          - string
          - 'null'
        firstName:
          type:
          - string
          - 'null'
        lastName:
          type:
          - string
          - 'null'
        avatarUrl:
          type:
          - string
          - 'null'
        isTeamMember:
          type: boolean
    GroupExpansionView:
      required:
      - groupId
      - isGroup
      - members
      type: object
      properties:
        groupId:
          type: string
        isGroup:
          type: boolean
        members:
          type: array
          items:
            $ref: '#/components/schemas/GroupExpansionView.GroupMemberView'
  securitySchemes:
    Authorization:
      type: oauth2
